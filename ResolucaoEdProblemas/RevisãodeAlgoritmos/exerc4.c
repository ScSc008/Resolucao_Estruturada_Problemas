#include <stdio.h>
#include <conio.h>

//4.	[FOR] Escreva um algoritmo em que leia 20 números e imprima a soma dos positivos 
//e o total de números negativos. 
void main{
	int n, i, soma_pos, cont;
	
	for(i=1; <=20; i++){
		printf("informe um numero: ");
		scanf("%d", &n);
		if(n < 0){
			cont+;
		}
		else{
			soma_pos += num; 
		}
	}
	printf("\n quantidade de numeros negativos: %d", cont);
	printf("\n resultado da soma dos numeros positivos: %d")
}

